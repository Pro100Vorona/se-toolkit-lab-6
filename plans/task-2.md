# План реализации Task 2 — Documentation Agent

- Добавить инструменты `read_file` и `list_files` с защитой от выхода за пределы проекта.
- Определить схемы инструментов для OpenAI function calling.
- Реализовать агентский цикл:
  - Отправляем вопрос + инструменты.
  - Если LLM запрашивает вызов – выполняем, добавляем результат в историю, повторяем.
  - Если ответ текстовый – извлекаем ответ и источник (по маркеру "Source:").
  - Лимит – 10 вызовов.
- Обновить `agent.py`.
- Обновить `AGENT.md`.
- Добавить два теста: 
  1. Вопрос про merge conflict → должен вызвать `read_file` с wiki/git-workflow.md.
  2. Вопрос про список файлов в wiki → должен вызвать `list_files`.
